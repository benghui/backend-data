# Backend Data Transformation, Design & Retrival

How to install and run

```shell
# Install NPM packages
npm install

# Run the database seed with database password
mysql -u root -p < seed.sql

# Start server
npm run start:dev
```
